# Environment Variables Template
# Copy this file to .env in the backend directory and fill in your actual values

# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com/
# Format: mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string_here

# Server Port (default: 8000)
PORT=8000

# JWT Secret Key
# Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=your_jwt_secret_key_here

# Cloudinary Configuration
# Get these from: https://cloudinary.com/console
CLOUD_NAME=your_cloudinary_cloud_name
API_KEY=your_cloudinary_api_key
API_SECRET=your_cloudinary_api_secret

# IMPORTANT NOTES:
# 1. Never commit the actual .env file to Git (it's in .gitignore)
# 2. For Render deployment, add these variables in the Render dashboard
# 3. Make sure MongoDB Atlas allows connections from 0.0.0.0/0 for Render
# 4. Keep your SECRET_KEY secure and never share it
